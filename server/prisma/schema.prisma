generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model User {
  id             String     @id @default(uuid())
  name           String
  email          String     @unique
  username       String     @unique
  gender         String
  password       String
  bio            String?
  image          String?
  website        String?
  interest       String?
  following      Following[] @relation("FollowerRelation")
  followingCount Int @default(0)
  followersCount Int @default(0)
  followers      Following[] @relation("FollowingRelation")
  posts          Post[]     @relation("UserPosts")
  likedPosts     Like[]     @relation("UserLikes")
  comments       Comment[]  @relation("Comments")
  matcherUser  Match[]    @relation("UserMatcher")
  matchedByUser Match[]    @relation("UserMatchedBy")
  matchesAsUserOne Matches[] @relation("MatchesUserOne")
  matchesAsUserTwo Matches[] @relation("MatchesUserTwo")
  sentMessages   Message[]  @relation("MessageSender")
  receivedMessages Message[]@relation("MessageReceiver")
  createdCommunities Community[] @relation("Creator")
  memberOfCommunities CommunityMembership[] @relation("UserCommunityMembership")
  taggedOnPosts     TaggingOnPost[] @relation("UserTagOnPost")
  taggedOnComments  TaggingOnComment[] @relation("UserTagOnComment")
writtenSecretNotes   SecretNotes[]         @relation("WriterOfSecretNotes")
  receivedSecretNotes  SecretNotes[]         @relation("ReceiverOfSecretNotes")
  createdAt      DateTime   @default(now())
}
model Community {
  id             String    @id @default(uuid())
  name           String
  description    String
  image          String?
  category       String
  creator        User      @relation("Creator", fields: [creatorId], references: [id])
  creatorId      String
  posts          Post[]    @relation("CommunityPosts")
  postsCount     Int       @default(0)
  members        CommunityMembership[] @relation("CommunityMembership")
  membersCount    Int       @default(0)
  createdAt      DateTime  @default(now())
}

model Post {
  id             String     @id @default(uuid())
  image          String?
  content        String
  creator        User       @relation("UserPosts", fields: [creatorId], references: [id])
  creatorId      String
  likes          Like[]     @relation("PostLikes")
  likesCount     Int        @default(0)
  comments       Comment[]  @relation("PostComments")
  community      Community? @relation("CommunityPosts", fields: [communityId], references: [id])
  communityId    String?
  commentsCount  Int         @default(0)
  taggedUsers    TaggingOnPost[]  @relation("PostUserTags")
  createdAt      DateTime   @default(now())
}
model Like {
  id        String    @id @default(uuid())
  user      User     @relation("UserLikes", fields: [userId], references: [id])
  userId    String
  post      Post      @relation("PostLikes",fields: [postId], references: [id])
  postId    String
  createdAt DateTime  @default(now())

  @@unique([userId, postId])
}

model Comment {
  id             String   @id @default(uuid())
  content        String
  creator        User     @relation("Comments", fields: [creatorId], references: [id])
  creatorId      String
  post           Post     @relation("PostComments", fields: [postId], references: [id])
  postId         String
  taggedUsers    TaggingOnComment[] @relation("CommentUserTags")
  createdAt      DateTime @default(now())
}


model TaggingOnPost {
  id        String   @id @default(uuid())
  post      Post     @relation("PostUserTags",fields: [postId], references: [id])
  postId    String
  user      User     @relation("UserTagOnPost", fields: [userId], references: [id])
  userId    String
  createdAt DateTime @default(now())

  @@unique([postId, userId])
}

model TaggingOnComment {
  id         String   @id @default(uuid())
  comment    Comment  @relation("CommentUserTags",fields: [commentId], references: [id])
  commentId  String
  user       User     @relation("UserTagOnComment", fields: [userId], references: [id])
  userId     String
  createdAt  DateTime @default(now())

  @@unique([commentId, userId])
}

model Following {
  id           String   @id @default(uuid())
  follower     User     @relation("FollowerRelation", fields: [followerId], references: [id])
  followerId   String
  following    User     @relation("FollowingRelation", fields: [followingId], references: [id])
  followingId  String
  createdAt    DateTime @default(now())
  @@unique([followerId, followingId])
}
model CommunityMembership {
  id           String   @id @default(uuid())
  user         User     @relation("UserCommunityMembership", fields: [userId], references: [id])
  userId       String
  community    Community @relation("CommunityMembership", fields: [communityId], references: [id])
  communityId  String
  joinedAt     DateTime @default(now())

  @@unique([userId, communityId])
}
model Match {
  id           String   @id @default(uuid())
  matcherUser  User     @relation("UserMatcher", fields: [matcherUserId], references: [id])
  matcherUserId String
  matchedByUser User    @relation("UserMatchedBy", fields: [matchedByUserId], references: [id])
  matchedByUserId String
  createdAt    DateTime @default(now())
  @@unique([matcherUserId, matchedByUserId])
}

model Matches {
  id       String @id @default(uuid())
  userOne  User   @relation("MatchesUserOne", fields: [userOneId], references: [id])
  userOneId String
  userTwo  User   @relation("MatchesUserTwo", fields: [userTwoId], references: [id])
  userTwoId String
  createdAt DateTime @default(now())
  @@unique([userOneId, userTwoId])
}

model Message {
  id         String   @id @default(uuid())
  sender     User     @relation("MessageSender", fields: [senderId], references: [id])
  senderId   String
  receiver   User     @relation("MessageReceiver", fields: [receiverId], references: [id])
  receiverId String
  message    String
  createdAt  DateTime @default(now())
}

model SecretNotes {
  id        String   @id @default(uuid())
  content   String
  writer    User?    @relation("WriterOfSecretNotes", fields: [writerId], references: [id])
  writerId  String?
  receiver  User     @relation("ReceiverOfSecretNotes", fields: [receiverId], references: [id])
  receiverId String
  createdAt DateTime @default(now())
}